# Codex Galaxy 3.0.5

- 修复 3.0.4 的启动渲染回归：删除重复标题行时，旧的账号列表渲染逻辑仍尝试写入已不存在的 `currentProfileName` 元素，空引用异常会中断后续渲染，因此账号记录和 API 排名看起来同时消失。
- 删除这处失效 DOM 写入，并增加回归断言，确保已移除的元素不会再次被 renderer 访问；既有本地账号和排名数据无需迁移即可重新显示。
- 本次升级不清理、不覆盖账号、认证、Codex 历史或排名数据；请直接在 3.0.4 上覆盖安装，不要删除本地配置。
- 保留最近 24 小时健康度、中文悬停原因、中文组件用途提示、健康度教程和前台低频轮询。
- 保留 gracefulTerminate 的 catch 兜底、本地日志、model_catalog_json、Windows sandbox 和模型目录兼容性检查；README、中英文应用内教程、文档和回归测试已同步更新。

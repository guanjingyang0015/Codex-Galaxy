# Codex Galaxy 1.5.0

## 修复

- 修复从 API 切换回官方账号后继续任务失败的问题。
- 清理中转服务产生的 `function_call` 外层 ID 不兼容值，保留工具调用内容和 `call_id`。
- API 到 API 的历史记录不会被这项兼容处理改写。

## 插件

- 新增本地插件市场自动扩展：选择包含 `marketplace.json` 的目录后，可批量安装其中有效插件。
- 保留本地插件安装和 Codex CLI 插件市场添加。
- 明确远程插件目录受 Codex 当前登录态和官方支持约束，不伪造权限。

## 文档与界面

- 中转站入口统一显示为“中转站 A / 中转站 B”，注册地址只在点击复制时使用。
- 精简中英文 README，并在顶部增加“下载最新版”入口。

# Codex Galaxy 1.10.1

## GPT-6 and API-to-API switching

- Extended GPT-family detection through GPT-9, so GPT-6 models receive the available reasoning intensity fallback when a relay omits explicit reasoning metadata.
- API → API switching now skips rollout-file scanning and rewriting. It updates the live provider, model, and required SQLite route metadata without processing gigabytes of historical files.
- Added Chinese and English API → API instructions to the README and the in-app phased tutorial.
- Added regression coverage for GPT-6 reasoning levels and the API-to-API no-rewrite path.
- The model catalog continues to preserve `model_catalog_json` metadata when the relay declares it.

## Preserved behavior

- Official ↔ API switching retains its existing compatibility and safety processing.
- Local history, SQLite data, credentials, and provider rollback behavior remain unchanged.

## Verification

- The redacted 本地日志 remains the recovery source of truth, and graceful shutdown failures are contained with a safe fallback. Windows sandbox settings remain documented and isolated.
- The API model catalog and model_catalog_json metadata remain preserved when the relay declares them.
- Documentation consistency, full tests, syntax checks, offline audit, and installer verification pass before publication.

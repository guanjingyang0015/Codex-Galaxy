# Codex Galaxy 1.10.2

## API-to-API provider repair

- Fixed the 1.10.1 fast API → API path: the live config now always receives the target API provider definition before Codex launches.
- SQLite provider/model route updates now work even when rollout files are intentionally skipped.
- Kept the performance improvement: API → API does not scan or rewrite gigabytes of rollout history.
- Added regression coverage for the missing-provider failure reported as `Model provider 'api-...' not found`.

## Preserved behavior

- GPT-6 reasoning intensity fallback remains available.
- Official ↔ API switching retains its existing compatibility and safety processing.
- Local history, credentials, rollback data, and `model_catalog_json` metadata remain protected.

## Verification

- The redacted 本地日志 remains the recovery source of truth; Windows sandbox settings remain documented and isolated.
- Documentation consistency, full tests, syntax checks, offline audit, and installer verification pass before publication.
